{
  "name": "super-core",
  "version": "1.0.0",
  "description": "Super Integrity",
  "main": "index.js",
  "scripts": {
    "clean": "lerna clean --yes && rm -rf node_modules",
    "bootstrap": "lerna bootstrap --concurrency ${CONCURRENCY:-2}",
    "install": "npm run bootstrap",
    "test": "lerna run test --stream --concurrency ${CONCURRENCY:-2}",
    "deploy": "lerna run deploy --stream --concurrency ${CONCURRENCY:-2}"
  },
  "keywords": [],
  "author": "engineforce@gmail.com",
  "license": "MIT",
  "devDependencies": {
    "@types/styled-components": "^4.1.18",
    "lerna": "^3.13.1",
    "typescript": "^3.6.2"
  }
}
